# Core
numpy
pandas
scikit-learn
scipy
Pillow
opencv-python
matplotlib
seaborn
tqdm

# Deep learning
# Pinned: the installed NVIDIA driver (550.163.01) tops out at CUDA 12.4 — an
# unpinned `torch` install can resolve to a cu13x build that silently fails
# torch.cuda.is_available() on this machine. If reinstalling, verify
# torch.cuda.is_available() is True afterward.
torch==2.6.0
torchvision==0.21.0
timm

# HF stack (TrOCR, VLM fine-tuning)
transformers
accelerate
peft
bitsandbytes
sentencepiece
huggingface_hub

# Augmentation
albumentations

# Metrics / text
jiwer
python-Levenshtein
editdistance

# Decoding / LM rescoring (optional, Phase 4)
pyctcdecode
https://github.com/kpu/kenlm/archive/master.zip#egg=kenlm

# Experiment tracking (optional)
tensorboard

# Testing
pytest
